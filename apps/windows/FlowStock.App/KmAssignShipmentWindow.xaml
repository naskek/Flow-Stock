<Window x:Class="FlowStock.App.KmAssignShipmentWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Коды КМ" Height="420" Width="640">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock x:Name="ItemText" FontWeight="SemiBold" />
        <TextBlock Grid.Row="1" x:Name="CountText" Margin="0,4,0,0" />
        <StackPanel Grid.Row="2">
            <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                <TextBox x:Name="CodeInput" Width="320" KeyDown="CodeInput_KeyDown" />
                <Button Content="Добавить" Click="Add_Click" Margin="8,0,0,0" />
            </StackPanel>
            <DataGrid x:Name="CodesGrid" AutoGenerateColumns="False" IsReadOnly="True" Margin="0,8,0,0">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Код" Binding="{Binding CodeDisplay}" Width="*" />
                    <DataGridTextColumn Header="Статус" Binding="{Binding StatusDisplay}" Width="140" />
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="Закрыть" IsCancel="True" />
        </StackPanel>
    </Grid>
</Window>
